// Simple placeholder summarizer (replace with actual logic or model call)
module.exports = function summarizeText(text) {
    // Dummy: Return first 3 sentences
    const sentences = text.split('. ');
    return sentences.slice(0, 3).join('. ') + '.';
  };
  